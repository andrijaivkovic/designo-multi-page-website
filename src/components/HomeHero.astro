---
import Button from "./Button.astro";
---

<section class="hero">
  <div class="hero__content">
    <h1>Award-winning custom designs and digital branding solutions</h1>
    <p>
      With over 10 years in the industry, we are experienced in creating fully
      responsive websites, app design, and engaging brand experiences. Find out
      more about our services.
    </p>
    <Button isLink={true} title="Learn more about us" to="/123">
      <p>Learn more</p>
    </Button>
  </div>

  <img
    class="hero__image"
    src="/images/home/image-hero-phone.png"
    alt="Hero phone mockup image"
  />

  <style>
    .hero {
      position: relative;

      display: flex;
      flex-direction: column;

      align-items: center;

      gap: 2.4rem;

      padding: 8rem 2.4rem 0 2.4rem;

      width: 100%;
      height: 90rem;

      background-color: var(--color-peach);

      overflow: hidden;

      background-image: url("/images/bg/bg-pattern-hero-home.svg");
      background-repeat: no-repeat;
      background-size: cover;
      background-position: -80% 10rem;

      @media screen and (min-width: 48em) {
        margin: 0 4rem;

        width: unset;

        border-radius: 1.5rem;

        padding: 6rem 10rem;

        height: 85rem;
      }

      @media screen and (min-width: 85.375em) {
        flex-direction: row;
        justify-content: space-between;

        gap: unset;

        margin: 0 11vw;
        padding: 14.5rem 10rem;

        height: unset;

        background-repeat: no-repeat;
        background-size: contain;
        background-position: right center;
      }

      @media screen and (min-width: 120em) {
        margin: 0 20vw;
      }
    }

    .hero__content {
      display: flex;
      flex-direction: column;

      align-items: center;

      gap: 2.4rem;

      color: var(--color-white);
      text-align: center;

      @media screen and (min-width: 85.375em) {
        align-items: start;
        text-align: start;
        max-width: 48rem;
      }
    }

    .hero__image {
      position: absolute;

      max-width: 70vw;
      height: auto;

      top: 55%;

      filter: drop-shadow(10px -20px 30px rgba(93, 2, 2, 0.5));

      @media screen and (min-width: 48em) {
        max-width: 28rem;
      }

      @media screen and (min-width: 85.375em) {
        position: static;

        justify-self: end;

        margin-bottom: -24rem;
        min-width: 30rem;
      }
    }
  </style>
</section>
